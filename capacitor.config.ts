import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh-24833-ion-select',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
